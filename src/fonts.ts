import { Poiret_One } from 'next/font/google'

// Import the font with specific weights or styles if needed
const poiretOne = Poiret_One({
  weight: '400',
  subsets: ['latin'],
})

export { poiretOne }